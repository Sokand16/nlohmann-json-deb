# Упаковка nlohmann/json в .deb-пакет

Этот проект автоматизирует создание **стандартного `.deb`-пакета** для популярной C++-библиотеки [nlohmann/json](https://github.com/nlohmann/json), а также предоставляет поддержку **Conan** для управления зависимостями. В комплекте — **тестовый C++-проект**, который проверяет корректность установки обоими способами.

## ✨ Возможности

- ✅ Создаёт корректный `.deb`-пакет (`libnlohmann-json-dev`) по стандартам Debian  
- ✅ Включает рецепт Conan для современного управления зависимостями  
- ✅ Содержит тестовое приложение для проверки работы библиотеки  
- ✅ Полностью автоматизирован через скрипт `build.sh`  
- ✅ Воспроизводим, документирован и готов к использованию в CI/CD  

## 📦 Зачем это нужно?

В профессиональной разработке (особенно в сферах с повышенными требованиями к безопасности, например, в криптографии) ПО должно быть:
- **Предсказуемо собираемым**
- **Безопасно упакованным**
- **Легко развёртываемым** на десятках машин

Этот проект демонстрирует все три принципа с использованием реальных инструментов: `dpkg`, `Conan`, `CMake`.

---

## 🛠 Требования

- Ubuntu 22.04 (или WSL2)
- Пакеты: `git`, `dpkg-dev`, `build-essential`
- Опционально: `conan` (устанавливается через `pip`)

Установите зависимости:
```bash
sudo apt update && sudo apt install -y git dpkg-dev build-essential
pip3 install conan  # опционально
```

## 🚀 Как собрать .deb-пакет
```bash
chmod +x build.sh
./build.sh
```

Результат:
✅ Файл nlohmann-json-3.11.3.deb — готов к установке командой sudo dpkg -i.

## 🔧 Поддержка Conan (опционально)
В проекте есть рецепт Conan для nlohmann_json/3.11.3.

Чтобы собрать и проверить:
```bash
conan create . --build=missing
conan list "nlohmann_json/*"
```

Теперь любой C++-проект может использовать эту библиотеку через conanfile.txt.

## 🧪 Тестовый проект: проверка установки
В папке test-project/ лежит простое C++-приложение, которое проверяет, что библиотека действительно работает после установки.

## Вариант 1: проверка через .deb
### 1.Установите пакет:
```bash
sudo dpkg -i nlohmann-json-3.11.3.deb
```

### 2.Соберите и запустите тест:
```bash
cd test-project
mkdir build && cd build
cmake ..
сmake --build .
./json-test
```

## Вариант 2: проверка через Conan
### 1.Перейдите в папку тестового проекта:
```bash
cd test-project
```

### 2.Установите зависимости и сгенерируйте toolchain:
```bash
conan install . --output-folder=build --build=missing
```

### 3.Соберите проект:
```bash
cd build
cmake .. -DCMAKE_TOOLCHAIN_FILE=conan_toolchain.cmake
cmake --build .
./json-test
```

✅ Ожидаемый вывод:
```bash
{
  "message": "Hello from DevOps!",
  "project": "nlohmann-json-deb"
}
```

Это доказывает, что ваша упаковка работает как в традиционном (.deb), так и в современном (Conan) подходе.

## 📁 Структура проекта
```
.
├── build.sh                # Основной скрипт автоматизации
├── conanfile.py            # Рецепт Conan для nlohmann/json
├── conandata.yml           # Ссылка на исходники и контрольная сумма
├── README.md
├── .gitignore
└── test-project/           # Тестовое C++-приложение
    ├── main.cpp
    ├── CMakeLists.txt
    └── conanfile.txt
```

## 📜 Лицензия
Этот проект: MIT
nlohmann/json: MIT License  
Библиотека разрабатывается и поддерживается в Европе. Проект нейтрален и безопасен для использования.

## 💼 Актуальность для DevOps
Этот проект напрямую соответствует задачам DevOps-инженера:

Упаковка C/C++-приложений для Linux.  
Работа с менеджером зависимостей Conan.  
Написание воспроизводимых скриптов (Bash).  
Валидация артефактов сборки.  
Подготовка ПО к развёртыванию.
